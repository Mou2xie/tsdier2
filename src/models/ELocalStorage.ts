// Definition: Enum for LocalStorage types

enum ELocalStorage {
    SELECTED_WORD_PCKAGE = 'local:selectedWordPackage',
    AUTO_SAVE_WORD = 'local:autoSaveWord',
    MUTE = 'local:mute',
    STOPSIDEPANEL = 'local:stopSidePanel'
}
export { ELocalStorage }